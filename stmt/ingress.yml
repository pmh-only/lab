apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: stmt-route
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: main
      namespace: gateway
  hostnames:
    - stmt.pmh.codes
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: ""
          kind: Service
          name: stmt-service
          port: 3000
          weight: 1
