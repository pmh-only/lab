apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: connect-route
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: main
      namespace: gateway
  hostnames:
    - connect.pmh.codes
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: ""
          kind: Service
          name: connect-service
          port: 8080
          weight: 1
