apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: notaikr-route
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: main
      namespace: gateway
  hostnames:
    - not.ai.kr
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: ""
          kind: Service
          name: notaikr-service
          port: 8080
          weight: 1
