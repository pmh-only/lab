apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: remote-route
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: main
      namespace: gateway
  hostnames:
    - remote.pmh.codes
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: ""
          kind: Service
          name: remote-guacamole
          port: 8080
          weight: 1
