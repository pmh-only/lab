apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: terms-route
spec:
  parentRefs:
    - name: main
      namespace: gateway
  hostnames:
    - terms.pmh.codes
  rules:
    - backendRefs:
        - name: terms
          port:
            number: 8080
