apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: terms-route
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: main
      namespace: gateway
  hostnames:
    - terms.pmh.codes
  rules:
    - backendRefs:
        - group: ""
          kind: Service
          name: terms
          port: 8080
