apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: radio-route
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: main
      namespace: gateway
  hostnames:
    - ads.pmh.codes
  rules:
    - backendRefs:
        - group: ""
          kind: Service
          name: pmhraspberry
          port: 1423
