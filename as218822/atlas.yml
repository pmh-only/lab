apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ripe-atlas
  namespace: as218822
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 100Mi
