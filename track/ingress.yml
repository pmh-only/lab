apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: track-route
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: main
      namespace: gateway
  hostnames:
    - track.pmh.codes
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: ""
          kind: Service
          name: track-service
          port: 3000
          weight: 1
