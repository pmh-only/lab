apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: allow-radio-ref
  namespace: tailscale
spec:
  from:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      namespace: radio
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      namespace: terms
  to:
    - group: ""
      kind: Service
      name: pmhraspberry
