apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: allow-radio-ref
  namespace: tailscale
spec:
  from:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      namespace: radio-route
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      namespace: terms-route
  to:
    - group: ""
      kind: Service
      name: pmhraspberry
