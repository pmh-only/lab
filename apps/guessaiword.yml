apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  annotations:
    argocd-image-updater.argoproj.io/guessaiword.update-strategy: digest
    argocd-image-updater.argoproj.io/image-list: guessaiword=ghcr.io/pmh-only/guessaiword:next
  name: guessaiword
  namespace: argocd
spec:
  destination:
    namespace: guessaiword
    server: https://kubernetes.default.svc
  project: default
  source:
    path: guessaiword
    repoURL: git@github.com:pmh-only/lab.git
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - PruneLast=true
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
