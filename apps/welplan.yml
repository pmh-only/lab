apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: welplan
  namespace: argocd
  annotations:
    argocd-image-updater.argoproj.io/welplan.update-strategy: digest
    argocd-image-updater.argoproj.io/image-list: welplan=ghcr.io/pmh-only/welplan:latest
spec:
  destination:
    namespace: welplan
    server: https://kubernetes.default.svc
  project: default
  source:
    path: welplan
    repoURL: git@github.com:pmh-only/lab.git
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - PruneLast=true
      - ApplyOutOfSyncOnly=true
      - CreateNamespace=true
