apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  annotations:
    argocd-image-updater.argoproj.io/image-list: here=ghcr.io/pmh-only/here:latest
    argocd-image-updater.argoproj.io/here.update-strategy: digest
  name: here
  namespace: argocd
spec:
  destination:
    namespace: here
    server: https://kubernetes.default.svc
  project: default
  source:
    path: here
    repoURL: git@github.com:pmh-only/lab.git
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ApplyOutOfSyncOnly=true
      - CreateNamespace=true
      - ServerSideApply=true
      - PruneLast=true
