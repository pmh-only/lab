apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: notaikr
  namespace: argocd
  annotations:
    argocd-image-updater.argoproj.io/notaikr.update-strategy: digest
    argocd-image-updater.argoproj.io/image-list: notaikr=ghcr.io/pmh-only/notaikr:latest
spec:
  destination:
    namespace: notaikr
    server: https://kubernetes.default.svc
  project: default
  source:
    path: notaikr
    repoURL: git@github.com:pmh-only/lab.git
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - PruneLast=true
      - ApplyOutOfSyncOnly=true
      - CreateNamespace=true
