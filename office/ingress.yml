apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: office-route
spec:
  parentRefs:
    - name: main
      namespace: gateway
  hostnames:
    - office.pmh.codes
  rules:
    - backendRefs:
        - name: office-service
          port: 80
