apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: thepool-route
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: main
      namespace: gateway
  hostnames:
    - pool.pmh.codes
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: "gateway.envoyproxy.io"
          kind: Backend
          name: thepool-service
          weight: 1
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: thepool-service
spec:
  endpoints:
    - fqdn:
        hostname: thepool-service.thepool.svc.cluster.local
        port: 8080
  tls:
    insecureSkipVerify: true
