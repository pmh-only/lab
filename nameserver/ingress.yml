apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nameserver-route
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: main
      namespace: gateway
  hostnames:
    - names.pmh.codes
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: ""
          kind: Service
          name: nameserver
          port: 5380
          weight: 1
