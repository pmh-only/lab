apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nameserver-route
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: main
      namespace: gateway
  hostnames:
    - names.pmh.codes
  rules:
    - backendRefs:
        - group: ""
          kind: Service
          name: nameserver
          port: 5380
