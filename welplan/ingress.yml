apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: welplan-route
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: main
      namespace: gateway
  hostnames:
    - welplan.pmh.codes
  rules:
    - backendRefs:
        - group: ""
          kind: Service
          name: welplan-service
          port: 3000
